class Vector2d:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector2d(self.x + other.x, self.y +other.y)

first = Vector2d(2,3)
second = Vector2d(3,4)
result = first + second

print(result.x)
print(result.y)
